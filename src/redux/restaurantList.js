import { RESTAURANTSFL } from '../shared/restaurantFlatlist';

export const RestaurantsFl = (state = RESTAURANTSFL, action) => {
    switch (action.type) {
        default:
            return state;
    }
};